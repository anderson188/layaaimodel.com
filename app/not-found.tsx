import { PageTitle } from "@/components/PageTitle";

export default function NotFound() {
  return (
    <PageTitle
      section="Page not found"
      lede="That path is not one of the six pages on this site."
    />
  );
}
